There's a bug either on the solution evaluation or in the algorithm. 
While doing the testing of phase 2 (@experiments/phase2/), I'm comparing the solution of the baseline and the solution of the algorithm. I'm using two Notebooks @Notebooks/compare_solutions.ipynb and @Notebooks/solution_evaluation.ipynb. Other notebooks can also be using the same logic as here. 
The issue is with the 'infeasible' metric calculation. I would normally expect the algorithm has 0 unfeasible labors (since it builds the solution under the same logic and parameters as the solution validation). However, in the @compare_solutions notebook, in the summary (build_overview_table) I see that there is 1 infeasible labor for the algorithm. Looking in the gantt, this is for the driver 211 with the labors 513445 and 513386. 
When I look at the same solution in the @solution_evaluation notebook, the summary (compute_payload_summary) doesn't show any labors_infeasible. And looking at the chart, there isn't any overlap as well. Looking in to detail, the distance for eveything else for both drivers is the same except for the driver move distance of the second involved labor. ON the compare_solution 16,06 and for the solution_evlatation is 14,26. In the latter, the solution seems to fit perfectly, as I would have expected from the algorithm's solution, therefore I believe that logic is coming from the algorithm (by output or by code). 