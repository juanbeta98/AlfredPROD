This will be a big augmentation in the code. There's a requirement for a special new feature. It's oriented towards the customer side at the time they're requesting a new service. The idea is to be able to, given the current schedule and the start and end positions of a new request, validate feasible time slots for the request.
The customer enters the positions of the labors and a desired time, and this logic will validate whether that time slot is feasible (by executing an insert or something similar) or not. If it is, is reported back as feasible, if not, then the time slots from the current time to the end of the day -given by intervals of 30 minutes-, check if assigning the service (all the labors) in each time slot and report the feasible slots back. In case of mulitlabor services, the time slots would correspond to the schedule_time of the first labor. All labors would follow chronologically in a downstream scheduling from there on. If there are more vt labors, these would also need to be feasible at the given time that they would occur. 
This is adjecent to the algorithm, since it will use all the assumptions and parameters, and surely a big part of the logic. This will also mean that there will be an aditional logic in nature. Therefore, an important part of the task is to design the repo structure to accommodate this new logic in a very clean an intuitive way, that follows industry standards and best practices. And also make sure that the current implementation can be properly connected to this new module.
Understand what I want, the actual requirement, the scructures that would be impacted by this and validate any thing you would like to clarify. 